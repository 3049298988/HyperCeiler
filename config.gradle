/*ext {
    CompileSdkVersion = 32  // 用于编译的SDK版本

    ApplicationId = "com.sevtinge.hyperceiler" //包名
    MinSdkVersion = 30    // 最低支持Android版本
    TargetSdkVersion = 32   // 目标版本
    VersionCode = 1006        //版本号
    VersionName = "1.0.0.6"
    isMinifyEnabled = false //是否混淆

    appReleaseDir = "D:\\apk\\"
    appReleaseTime =  new Date().format("yyyyMMddHHmm")
}*/


ext {
    BuildTypes = "debug"
    LibraryPath = "../library/"

    //公共的(系统的)
    roots = [
            /*"animation" : "libs/animation-${BuildTypes}.aar",*/
            "appcompat" : "${LibraryPath}appcompat-${BuildTypes}.aar",
            "core" : "${LibraryPath}core-${BuildTypes}.aar",
            "folme" : "${LibraryPath}folme-${BuildTypes}.aar",
            "nestedheader" : "${LibraryPath}nestedheader-${BuildTypes}.aar",
            "haptic" : "${LibraryPath}haptic-${BuildTypes}.aar",
            "preference" : "${LibraryPath}preference-${BuildTypes}.aar",
            "recyclerview" : "${LibraryPath}recyclerview-${BuildTypes}.aar",
            "smooth" : "${LibraryPath}smooth-${BuildTypes}.aar",
            "springback" : "${LibraryPath}springback-${BuildTypes}.aar",
            "graphics" : "${LibraryPath}graphics-${BuildTypes}.aar"
            /*"external" : "libs/external-${BuildTypes}.aar",
            "os" : "libs/os-${BuildTypes}.aar"*/
    ]
}
